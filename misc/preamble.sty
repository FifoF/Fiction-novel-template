\usepackage{microtype}
\usepackage{lettrine}
\usepackage{graphicx}
\usepackage{ebgaramond}
\usepackage{ragged2e} % Hyphenate ragged-right text; used in copyright page
                      % for smoother right margin            (\RaggedRight)

\input{misc/ushyphex}  % Hyphenation exceptions

\newcommand{\hsp}{\kern 1pt}    % hair space for nested quotation marks

% Line spacing
\renewcommand{\baselinestretch}{1.125}
\setlength{\parskip}{0pt}
\setlength{\parindent}{1.25em}
\widowpenalty=10000   % Reduce widows
\clubpenalty=10000    % and orphans

% Page size
\usepackage[
    paperheight=9in,
    paperwidth=6in,
    top=1in,
    bottom=1in,
    outer=0.75in,
    inner=1in
]{geometry}

% Reduce overfull \hbox{}es
\sloppy

% Contents page
\usepackage[titles]{tocloft}
\renewcommand{\cftchapfont}{\normalfont}
\renewcommand{\cftchappagefont}{\normalfont}

% chapter headings
\renewcommand\thechapter{\Roman{chapter}}
\usepackage[center,sc]{titlesec}
%\renewcommand{\chaptername}{Adventure}

% headers/footers
\usepackage{fancyhdr}
\setlength{\headheight}{15pt}
\pagestyle{fancy}
\fancyhf{}
\renewcommand{\headrulewidth}{0pt}
%\fancyhf[CFE,CFO]{\thepage}         % Set page numbers in the footer
\fancyhead[LE,RO]{\thepage}        % Set page numbers in the left/right sides of the header
\fancyhead[CE]{\itshape\authorname}
\fancyhead[CO]{\itshape\booktitle}